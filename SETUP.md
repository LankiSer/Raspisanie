# Быстрый запуск Schedule SaaS

## Что исправлено

✅ **Модели данных** - приведены к единому стандарту, убраны дублирования  
✅ **Единая миграция** - создана миграция `001_create_unified_tables.py` со всеми таблицами  
✅ **Исправленный сидер** - новый `scripts/seed.py` с корректными данными  
✅ **Docker автоинициализация** - при первом запуске автоматически создается БД с тестовыми данными  

## Запуск проекта

### Первый запуск (с автоматической инициализацией)

```bash
# Клонируйте проект и перейдите в папку
cd rasp

# Запустите все сервисы
docker-compose up --build
```

Система автоматически:
1. 🗄️ Создаст базу данных PostgreSQL
2. 🔧 Выполнит миграции (создание всех таблиц)  
3. 🌱 Загрузит тестовые данные через сидер
4. 🚀 Запустит приложение

### Доступы к системе

После запуска откройте:
- **Frontend**: http://localhost  
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/api/v1/docs

### Тестовые пользователи

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@university.edu | admin123 |
| Методист | methodist@university.edu | methodist123 |
| Преподаватель | teacher1@university.edu | teacher123 |

## Структура данных

При инициализации создаются:
- 🏛️ 1 организация (Московский технический университет)
- 👥 3 группы студентов (ИУ5-61Б, ИУ5-62Б, ИУ6-61Б)
- 👨‍🏫 3 преподавателя
- 📚 5 курсов (Математика, Python, БД, Графика, Английский)
- 🏢 6 аудиторий
- ⏰ 5 временных слотов (пары)
- 📅 Примеры занятий

## Полезные команды

```bash
# Остановить все сервисы
docker-compose down

# Пересоздать с очисткой данных
docker-compose down -v && docker-compose up --build

# Посмотреть логи backend
docker-compose logs -f backend

# Войти в контейнер backend
docker-compose exec backend bash

# Запустить миграции вручную
docker-compose exec backend alembic upgrade head

# Запустить сидер вручную  
docker-compose exec backend python scripts/seed.py
```

## Архитектура

- **Frontend**: Vue 3 + Vite + Tailwind CSS
- **Backend**: FastAPI + SQLAlchemy + Alembic
- **Database**: PostgreSQL 16
- **Cache**: Redis 7
- **Containerization**: Docker + Docker Compose

Система готова к разработке и тестированию! 🎉
